\newenvironment{prealable}[0]{%
	\paragraph{Pré-requis}
	\itshape\footnotesize%
	}{%
	\upshape\normalsize%
	}

\newenvironment{anedocte}[0]{%avant
	\vspace*{0.5cm}%
	\paragraph{Anedocte}%
	\begin{adjustwidth}{4em}{}%
	%
	\footnotesize%
	%\paragraph{Anedocte}
	}
	{%apres
	\normalsize%
	\end{adjustwidth}%
	\vspace*{0.5cm}%
	}
\newenvironment{attention}[0]{%avant
	\vspace*{0.5cm}%
	\paragraph{attention}%
	\begin{adjustwidth}{4em}{}%
	%
	\footnotesize%
	%\paragraph{Anedocte}
	}
	{%apres
	\normalsize%
	\end{adjustwidth}%
	\vspace*{0.5cm}%
	}


\newenvironment{nomarge}[0]{%avant
\begin{changetext}{}{}{0cm}{0cm}{}sss%
	}%
{%fin
	\end{changetext}}
%\newenvironment{code}[0]{%avant
%	\begin{minted}[linenos]{latex}%
%	}%
%	{%après
%	\end{minted}%
%	}%
% Repris du package btdxdocx

\makeatletter

\newenvironment*{fieldlist}
  {\list{}{%
     \setlength{\labelwidth}{\marglistwidth}%
     \setlength{\labelsep}{\marglistsep}%
     \setlength{\leftmargin}{0pt}%
     \renewcommand*{\makelabel}[1]{\hss\marglistfont##1}}%
   \def\fielditem##1{%
     \item[##1]%
     \ltd@pdfbookmark{##1}{##1}%
     %field (##2)\par\nobreak
     \vspace{\itemsep}}%
   \def\listitem##1##2{%
     \item[##1]%
     \ltd@pdfbookmark{##1}{##1}%
     list (##2)\par\nobreak
     \vspace{\itemsep}}}
  {\endlist}
 \makeatother

